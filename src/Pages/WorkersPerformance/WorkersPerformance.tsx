import { NavProps } from "../../Utils/Types";

const WorkersPerformancePage: React.FC<NavProps> = (props: NavProps) => { 
    props.setCategory("teams")

    return ( 
        <div className="workersperformance-root">
            
        </div>
    )
}

export default WorkersPerformancePage; 
