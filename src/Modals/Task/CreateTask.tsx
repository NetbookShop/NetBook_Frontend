import { ModalProps } from "../../Utils/Types";

const CreateTaskModal: React.FC<ModalProps> = (props: ModalProps) => {
    return ( 
        <div className="create-task-root">
            
        </div>
    )
}

export default CreateTaskModal; 
